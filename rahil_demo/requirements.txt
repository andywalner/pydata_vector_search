# ML / DL stack (Apple Silicon-friendly)
torch>=2.3.0
torchvision>=0.18.0
timm>=1.0.9
scikit-learn>=1.4.2
numpy>=1.26.0
pillow>=10.3.0

# Plotting
matplotlib>=3.8.0

# Optional: If you use Hudi CLI utilities or additional connectors
# pyarrow>=16.0.0

# NOTE:
# - Torch 2.3+ includes Metal (MPS) backend for Apple Silicon by default.
# - Use the bundled Spark jars for Hudi + Lance instead of pip packages:
#     /path/to/hudi-spark3.5-bundle.jar
#     /path/to/lance-spark-bundle-3.5_2.12-0.0.14.jar
